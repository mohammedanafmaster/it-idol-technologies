// Declare the variable and take input from the user
let number = prompt("Enter a number: ");

// Create the logic for multiplication table and print the output
for (let i=1 ; i<=10 ; i++) {
    console.log(`${number} x ${i} = ${number * i}`);
}