// Take number as an input from the user
let number = prompt("Enter a number: ");

// Multiply number with values from 1 to 10 and print the output on the console
for (let i=1 ; i<=10 ; i++) {
    console.log(`${number} x ${i} = ${number*i}`)
}